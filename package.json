{
  "name": "md-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm --filter @md/web build",
    "start": "pnpm --filter @xlsama/md start",
    "test": "pnpm -r test",
    "lint": "oxlint --fix",
    "release": "bumpp -r",
    "publish": "pnpm --filter @xlsama/md publish --access public",
    "publish:dry-run": "pnpm --filter @xlsama/md publish --access public --dry-run"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "bumpp": "^12.2.1",
    "oxlint": "^1.78.0",
    "oxlint-tsgolint": "^7.0.2001",
    "typescript": "^7.0.2"
  }
}
