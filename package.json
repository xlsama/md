{
  "name": "md-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm --filter @md/web build",
    "start": "pnpm --filter mdopen start",
    "test": "pnpm -r test",
    "lint": "oxlint --fix",
    "release": "bumpp -r"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "bumpp": "^12.2.0",
    "oxlint": "^1.78.0",
    "oxlint-tsgolint": "^7.0.2001",
    "typescript": "^7.0.2"
  }
}
